class Card {
  constructor(logo, name, vacancy, region, filters, date, isliked) {
    logo = this.logo;
    name = this.name;
    vacancy = this.vacancy;
    region = this.region;
    filters = this.filters;
    isliked = isliked;
  }

  // displayCard() {
  //  document.getElementById(bigCard).innerHTML = 
  //}
}

